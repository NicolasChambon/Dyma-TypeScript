// let sumTwoNb: (nb1: number, nb2: number) => number;

// sumTwoNb = (a: number, b?: number) => {
//   if (b) {
//     return a + b;
//   }
//   return a;
// };

//---------------

// let sumTwoNb = (...numbers: number[]) => { // ...param est forcemment un tableau
//   return numbers.reduce((acc, number) => { // number est la valeur courante du tableau
//     acc += number;
//     return acc;
//   }, 0);
// };

// sumTwoNb(1, 2, 3, 4);

//---------------

// function addition(a: number, b: number): number;
// function addition(a: string, b: string): string;
// function addition(a, b): string | number {
//   return a + b;
// }

//---------------

// const foo = addition(1, 2);
// const bar = addition('hello ', 'world');

// bar.split('');
// bar.length;
